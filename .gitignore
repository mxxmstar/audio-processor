# Tauri / Rust
src-tauri/target/
src-tauri/gen/

# Node
node_modules/
dist/

# Legacy Python
.venv/
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# 图标生成临时源（真正图标在 src-tauri/icons/）
src-tauri/icon-src.png

# Keep test sample
!*.mp3

# 运行时资源：保留 bin/ 目录与 fpcalc.exe，忽略 ffmpeg（体积大，由用户自行放置）
!bin/
bin/fpcalc.exe
bin/ffmpeg.exe
bin/ffmpeg
bin/ffmpeg-*
bilidown/
bilidownload/

# 登录态持久化（含 SESSDATA，切勿提交）
src-tauri/auth.json
src-tauri/wbi_cache.json
auth.json
wbi_cache.json

# 下载临时产物（音视频分别下载 / 合并中间文件）
*.audio.m4a
*.video.mp4
*.audio.tmp
*.video.tmp

# 历史记录数据库（本地 SQLite，不入库）
history.db
**/history.db